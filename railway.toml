[build]
builder = "NIXPACKS"
buildCommand = "rm -f package-lock.json && npm install --include=dev && npm run build && npm run setup"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
